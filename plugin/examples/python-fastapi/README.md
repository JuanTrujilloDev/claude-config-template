# python-fastapi example

FastAPI service, no frontend. Tools: ruff, mypy, pytest.

```bash
~/code/claude-config-template/setup.sh \
  --target . \
  --answers ./examples/python-fastapi/answers.env
```

Adjust `project_name`, `project_slug`, and `project_description` before running.
